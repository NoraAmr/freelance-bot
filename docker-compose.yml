version: "3.9"
services:
  mostaql-bot:
    build: .
    restart: unless-stopped
    environment:
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - CHAT_ID=${CHAT_ID}
      - CHECK_INTERVAL=120
    volumes:
      - ./seen_jobs.json:/app/seen_jobs.json
